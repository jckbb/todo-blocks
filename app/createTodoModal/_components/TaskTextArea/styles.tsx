import { StyleSheet } from "react-native";

export default StyleSheet.create({
  textarea: {
    minHeight: 120,
    padding: 16,
    marginHorizontal: 16,
    borderRadius: 16,
    backgroundColor: "lightgrey",
  },
});
